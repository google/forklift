# Forklift: Vue.js TodoMVC example

## Test locally

1. `yarn install`
2. `yarn run dev` runs the app in development mode
3. Open <http://localhost:8080/> to view it in the browser.

## Build

1. `yarn run build`
